import { musicCommand } from "../utils/musicCommand.js";
export const { data, execute, executePrefix } = musicCommand("remove", "Elimina una posición de la cola", "remove", "posicion", "Número de la canción");
