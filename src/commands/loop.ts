import { musicCommand } from "../utils/musicCommand.js";
export const { data, execute, executePrefix } = musicCommand("loop", "Configura el loop: off, song o queue", "loop", "modo", "off, song o queue");
