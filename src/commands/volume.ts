import { musicCommand } from "../utils/musicCommand.js";
export const { data, execute, executePrefix } = musicCommand("volume", "Cambia el volumen (0-100)", "volume", "nivel", "Volumen entre 0 y 100");
