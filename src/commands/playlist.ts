import { musicCommand } from "../utils/musicCommand.js";
export const { data, execute, executePrefix } = musicCommand("playlist", "Gestiona playlists en memoria", "playlist", "accion", "crear, añadir, cargar, ver o borrar");
