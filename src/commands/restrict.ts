import { musicCommand } from "../utils/musicCommand.js";
export const { data, execute, executePrefix } = musicCommand("restrict", "Restringe la música a un canal de voz", "restrict", "canal", "ID del canal");
